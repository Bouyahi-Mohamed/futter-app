import 'package:flutter/material.dart';
import 'hero_game_map_screen.dart';

class HeroGameScreen extends StatelessWidget {
  const HeroGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Navigate directly to the Hero Game Map
    return const HeroGameMapScreen();
  }
}
