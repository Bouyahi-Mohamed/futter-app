import 'package:flutter/material.dart';
import 'city_game_map_screen.dart';

class CityGameScreen extends StatelessWidget {
  const CityGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Navigate directly to the City Game Map
    return const CityGameMapScreen();
  }
}
